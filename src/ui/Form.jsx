import styled from 'styled-components';

const Form = styled.form`
  font-size: 1.4rem;
  overflow: hidden;
  width: 60rem;
  :not(:last-child) {
    margin-bottom: 1rem;
  }
`;

export default Form;
